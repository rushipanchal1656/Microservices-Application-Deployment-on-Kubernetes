apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: microservices
spec:
  storageClassName: local-path   # ⭐ ADD THIS LINE
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi